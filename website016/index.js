// ARRAYS

let fruits = ["apple", "banana", "orange", "grape"];

fruits.push("cocunut") // Isso adiciona um elemento a mais
fruits.pop() // Isso tira o ultimo elemento colocado
fruits.sort() // Organiza em ordem alfabetica
fruits.sort().reverse() // Deixa em ordem ao contrario

for(let fruit of fruits) {
   console.log(fruits);
}

